# Известные ошибки и найденные проблемы

Лог багов, найденных в ходе разработки. Отмечаем: статус, причина, как решено.

## Исправлено

### 1. Незакрытый div ломал вёрстку (index.html)
- **Симптом:** незакрытый `</div>` после `view-container` — разметка могла «разъезжаться».
- **Причина:** пропущен закрывающий тег.
- **Решение:** добавлен недостающий `</div>`.
- **Статус:** исправлено, проверено E2E-тестами и `npm run build`.

### 2. Prettier не мог разобрать HTML-шаблонные строки в js/app.js
- **Симптом:** `npm run format`/`format:check` падал с parse error.
- **Причина:** в скрипте есть закрывающие `</script>`-подобные последовательности в JS-шаблонных строках (в т.ч. внутри `renderSubjectNotes` и др.).
- **Решение:** настроен `prettier.ignore` для `js/app.js` + `index.html` (см. `.prettierignore`).
- **Статус:** исправлено.

### 3. Моноип/ранние JS-ошибки при загрузке
- **Симптом:** устаревшие `onclick`-атрибуты и порядок инициализации.
- **Причина:** монолитная инициализация на `DOMContentLoaded`.
- **Решение:** глобальная `init()` перерегистрирует все обработчики; все ссылки на элементы пересоздаются через `querySelectorAll`.
- **Статус:** исправлено; подтверждено E2E-тестами.

### 4. Дублирование элементов `#loginBtn`/`#logoutBtn` в сайдбаре
- **Симптом:** strict-mode-ошибка Playwright: `#loginBtn` resolved to 2 elements.
- **Причина:** `renderAuthHeader()` добавлял новый элемент в `#authHeaderArea` без очистки — при смене состояния (гость → авторизован) в DOM копились и кнопка «Войти», и бейдж пользователя.
- **Решение:** перед добавлением `authArea.innerHTML = ""` (срабатывает только при изменении сигнатуры пользователя, т.к. есть guard `lastAuthSignature`).
- **Статус:** исправлено.

### 5. После регистрации повторный вход отправлял `/register` (409)
- **Симптом:** E2E «авторизация: регистрация, вход, выход» — после регистрации и выхода повторный вход не срабатывал, модалка не закрывалась, тост «Пользователь с таким email уже существует».
- **Причина:** `authMode` оставался `"register"` после успешной регистрации — форма при повторном открытии снова шла на `/api/auth/register`.
- **Решение:** после успешной регистрации `authMode` сбрасывается в `"login"` и текст заголовка/кнопок модалки возвращается к режиму входа.
- **Статус:** исправлено.

### 6. E2E зависали в параллельном режиме
- **Симптом:** при `fullyParallel` и дефолтных воркерах (12) тесты падали по таймауту 30с в `beforeEach`/teardown («Fixture trace recording timeout»).
- **Причина:** 12 параллельных Chromium не влезали в 8 ГБ RAM (свободно ~2.7 ГБ) — браузеры зависали.
- **Решение:** `workers: 3` в `playwright.config.js` (в CI — 2).
- **Статус:** исправлено.

### 7. Back/forward: Chromium схлопывает соседние записи истории с одинаковым URL
- **Симптом:** E2E «навигация кнопками браузера» — `goBack` с `#notes` приземлялся не на subject-detail, а сразу на subjects (запись subject-detail пропускалась).
- **Причина:** sub-view пушился через `history.pushState(state, "", location.hash)` — URL `/#subjects` совпадал с URL предыдущей записи; Chromium объединяет/пропускает соседние same-document записи с одинаковым URL.
- **Решение:** sub-view получают уникальные hash-URL: `#subject-detail:<id>`, `#note-reader:<subjectId>:<noteId>`, `#quiz-player`, `#quiz-results`; `pushSubView(state, hash)` принимает URL явно.
- **Статус:** исправлено, проверено E2E-тестом.

### 8. E2E back/forward: тест ожидал нестандартную семантику истории
- **Симптом:** после `push → back → push(другая вью)` `goBack` шёл не в первую вью, а в начальную.
- **Причина:** это стандартное поведение браузера — новый `history.pushState()` обрезает forward-записи; тест был написан «от желаемого», а не от реальной семантики.
- **Решение:** тест переписан под стандартную семантику: `Forward` проверяется до нового pushState.
- **Статус:** исправлено (поправлен тест, а не приложение).

### 9. E2E авторизации не был идемпотентным (регистрация 409 при повторе)
- **Симптом:** второй прогон E2E «авторизация» падал — модалка не закрывалась после нажатия `#authSubmitManual`.
- **Причина:** фиксированный email `ivan@example.ru` оставался в `data/examhub.test.db` между прогонами; `POST /api/auth/register` возвращает 409 «Пользователь с таким email уже существует».
- **Решение:** email генерируется уникальным за прогон (`ivan${Date.now()}@example.ru`), имя для проверки выводится из email (`expectedName`).
- **Статус:** исправлено.

### 10. Утечка данных участников групп репетитора (IDOR / Недостаточная авторизация)
- **Симптом:** `GET /api/teacher/groups/:id` мог вернуть данные группы любому авторизованному пользователю.
- **Причина:** Отсутствовала проверка прав доступа.
- **Решение:** В [server/routes/teacher.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/server/routes/teacher.js) добавлена проверка прав `isTeacher || isMember`.
- **Статус:** исправлено.

### 11. Динамический сброс/обновление UI Кабинета Репетитора при входе/выходе
- **Симптом:** При входе/выходе Кабинет учителя не перерисовывался автоматически.
- **Причина:** Отсутствовал вызов `renderTeacherCabinet()`.
- **Решение:** Добавлен вызов `renderTeacherCabinet()` и `renderStudentAssignments()` в [js/modules/auth.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/js/modules/auth.js).
- **Статус:** исправлено.

### 12. Стили тёмной темы для модалок и мобильной панели
- **Симптом:** Мобильная панель и модалки оставались белыми в тёмной теме.
- **Причина:** Отсутствовали правила `[data-theme="dark"]` для модальных окон и мобильной навигации.
- **Решение:** В [css/style.css](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/css/style.css) добавлены стили тёмной темы.
- **Статус:** исправлено.

### 13. Фокус для клавиатурной навигации (`:focus-visible`) и состояние `:disabled`
- **Симптом:** Отсутствовал визуальный фокус Tab и стиль отключенных кнопок.
- **Причина:** Отсутствовали правила `:focus-visible` и `button:disabled`.
- **Решение:** Добавлены стили `:focus-visible` и `button:disabled` в [css/style.css](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/css/style.css).
- **Статус:** исправлено.

### 15. Коллизия UNIQUE constraint в videos.id при сидинге базы данных
- **Симптом:** При выполнении `node server/seed.js` возникала ошибка `UNIQUE constraint failed: videos.id`.
- **Причина:** В `js/data.js` у нескольких тем использовался одинаковый `youtubeId: "dQw4w9WgXcQ"`. В `server/seed.js` в качестве primary key `id` таблицы `videos` передавалось значение `v.youtubeId`, из-за чего повторные вставки вызывали коллизию уникального ключа SQLite.
- **Решение:** В `server/seed.js` в качестве первичного ключа `id` используется `v.id || `${topic.id}_video``, а сама ссылка на YouTube хранится в поле `youtube_id`. Запросы на вставку во все таблицы переведены на синтаксис SQLite UPSERT (`ON CONFLICT... DO UPDATE SET...`).
- **Статус:** исправлено, проверено юнит-тестами `tests/unit/m3_verification.test.js` и повторным вызовом `node server/seed.js`.

### 16. Утечка исходников и данных через статическую раздачу корня
- **Симптом:** `GET /server/index.js`, `GET /data/examhub.db`, `GET /package.json`, `GET /tests/e2e/smoke.spec.js` возвращали 200 с содержимым файлов (эндпоинт `.env` отдавал 404 только из-за того, что Express игнорирует dot-файлы).
- **Причина:** `app.use(express.static(config.root))` раздавал всю папку проекта, включая серверный код, БД и тесты.
- **Решение:** В [server/index.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/server/index.js) статика ограничена allowlist-ом `FRONTEND_ASSETS = ["/js", "/css", "/index.html"]` (плюс `/`); всё остальное → 404.
- **Статус:** исправлено, проверено прямыми запросами к URL и E2E-тестами.

### 17. Хардкод прогресса, streak и аналитики вместо реального учёта
- **Симптом:** «Вы занимаетесь 23 дня подряд 🔥», «Марафонец (23 дня подряд)», «100% изучено» на конспектах и фейковая формула процентов в аналитике были статикой/демо-данными; чекбоксы плана считали `checkedCount = 2` хардкодом.
- **Причина:** в [js/modules/state.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/js/modules/state.js) и [js/modules/plan.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/js/modules/plan.js) демо-цифры, streak не вычислялся по датам, темы не помечались прочитанными, план не сохранялся.
- **Решение:** добавлены `stats.readTopics/lastActiveDate/planTasks`, `registerActivity()` (streak по датам), `markTopicRead()/isTopicRead()/getSubjectProgress()/setPlanTaskDone()`; конспекты помечаются «Изучено», streak обновляется в уведомлениях и аналитике, аналитика считает реальные проценты, план восстанавливается и сохраняется.
- **Статус:** исправлено, проверено юнит-тестами и E2E.

### 18. Мёртвая кнопка «Выполнить» в заданиях репетитора
- **Симптом:** кнопка «Выполнить» в студенческих заданиях не открывала тест, а при завершении теста результат никуда не отправлялся.
- **Причина:** на кнопке не было обработчика, отсутствовал серверный маршрут приёма результатов.
- **Решение:** кнопка получила класс/данные и запускает `startAssignmentQuiz(...)` с `appState.pendingAssignmentId`; при завершении теста в [js/modules/quiz.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/js/modules/quiz.js) — `POST /api/teacher/assignments/:id/submit`; на бэке добавлен роут в [server/routes/teacher.js](file:///c:/Users/мишка/Desktop/сайтик_бахчасарай/server/routes/teacher.js) с валидацией и upsert в `assignment_submissions`.
- **Статус:** исправлено, проверено юнит-тестами и E2E.

## Открытые вопросы
- Содержимое конспектов/вопросов — учебный контент, динамически пополняется.
- Платежи Premium — mock-заглушка для тестирования без внешнего эквайринга.

